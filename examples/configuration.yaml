# Example configuration.yaml entry
# This integration uses config flow (UI configuration)
# No YAML configuration is needed

# However, you can customize the scan interval:
poolcopilot:
  scan_interval: 300  # Update every 5 minutes (default)
